CREATE TABLE customer (
  id VARCHAR(50) NOT NULL NOT NULL,
  first_name VARCHAR(50) NOT NULL,
  last_name VARCHAR(50) NOT NULL,
  email_address VARCHAR(50) NOT NULL,
  credit_card_number VARCHAR(255) NOT NULL,
);